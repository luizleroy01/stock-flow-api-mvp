services:
  - type: web
    name: stock-flow-api-mvp
    env: python
    region: oregon
    plan: free
    buildCommand: ""
    startCommand: gunicorn app:app
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: stock_flow_db_myrs
          property: connectionString

databases:
  - name: stock_flow_db_myrs
    plan: free
